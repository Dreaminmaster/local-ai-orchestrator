# Web App

The current MVP uses `frontend/` as a static web console served by FastAPI.

This directory is reserved for a future framework-based web app if the console
is migrated to React/Vue/Svelte.
