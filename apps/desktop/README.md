# Desktop App Placeholder

The final product is designed as an independent desktop application.

This directory is reserved for a future Tauri or Electron shell that wraps the
existing web task workbench and connects it to OS-level desktop automation.

Recommended next step:

```bash
npm create tauri-app@latest apps/desktop
```

Then configure the shell to load the FastAPI-served frontend or bundle a static build.
