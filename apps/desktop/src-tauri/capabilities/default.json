{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop shell permissions for the local web UI.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
